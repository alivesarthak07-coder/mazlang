export type Word = {
  id: string;
  mazlang: string;
  meaning: string;
  approved_at: string;
};

export type Suggestion = {
  id: string;
  mazlang: string;
  meaning: string;
  created_at: string;
};
